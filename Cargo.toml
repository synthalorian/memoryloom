[package]
name = "memoryloom"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"
description = "Local transcript indexing and literal search for agent memory"

[dependencies]
